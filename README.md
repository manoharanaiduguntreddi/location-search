# Location Search (Spring Boot + Thymeleaf)

Implements the JYSK case study with a login form and **multi-city per ZIP** support.

## Run
```bash
mvn spring-boot:run
```
Visit `http://localhost:8080/` → login with any user/password → search.
